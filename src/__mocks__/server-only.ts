// Stub for `server-only` in Vitest (jsdom) environment.
// The real package throws on client-side import; this no-op lets server
// modules be unit-tested without a full Next.js server context.
export {};
